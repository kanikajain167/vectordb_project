# vectordb_project
A vector database implemented from scratch in NumPy, featuring exact and approximate (IVF-Flat) similarity search, a CRUD-style API, and recall/latency benchmarking on 50,000+ vectors.
